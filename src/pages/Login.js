import styled from 'styled-components';
import AuthLayout from '../components/common/AuthLayout';

const Login = () => {
  return <AuthLayout title="로그인">로그인</AuthLayout>;
};

const Container = styled.div`
  background-color: #fff;
`;

export default Login;
